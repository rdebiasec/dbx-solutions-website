# Archived locale JSON (not used at runtime)

These files were an older content model. **Live copy is in `../site-app.js`** with ES/PT in `../../i18n-runtime.js` and `../pt-entries.js`.

Do not wire these back into the build.
